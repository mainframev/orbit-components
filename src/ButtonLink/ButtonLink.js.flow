// @flow
import { defaultTokens } from "@kiwicom/orbit-design-tokens";

type Type = "primary" | "secondary";
type Size = "small" | "normal" | "large";

export type Props = {|
  +children?: React$Node,
  +component?: string | React$Node,
  +onClick?: (e: SyntheticEvent<HTMLButtonElement>) => void | Promise<any>,
  +disabled?: boolean,
  +block?: boolean,
  +external?: boolean,
  +type?: Type,
  +size?: Size,
  +href?: string,
  +width?: number,
  +icon?: React.Node,
  +theme?: typeof defaultTokens,
  +transparent?: boolean,
|};

type styledButtonLink = {
  component?: string | React$Node,
  children?: React$Node,
  href?: string,
  bordered?: boolean,
  disabled?: boolean,
  block?: boolean,
  type?: Type,
  size?: Size,
  width?: number,
  icon?: React$Node,
  transparent?: boolean,
  onlyIcon?: boolean,
  tokens?: { [key: string]: string | number },
  theme?: typeof defaultTokens,
};
declare export var StyledButtonLink: React$ComponentType<styledButtonLink>;

declare export default React$ComponentType<Props>;
